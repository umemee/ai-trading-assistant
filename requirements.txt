=====================================================================
# requirements.txt - AI Trading Assistant V5.1
# =====================================================================
# 이 파일은 프로젝트에 필요한 모든 Python 패키지와 정확한 버전을 정의합니다.
# 설치 방법: pip install -r requirements.txt

# =================================================================
# 🚀 FastAPI & 웹 서버 관련
# =================================================================
fastapi==0.104.1                    # 현대적인 웹 API 프레임워크
uvicorn[standard]==0.24.0            # ASGI 서버 (WebSocket 지원)
python-multipart==0.0.6             # 파일 업로드 및 폼 데이터 처리
websockets==12.0                    # WebSocket 실시간 통신
starlette==0.27.0                   # FastAPI 기반 프레임워크
python-jose[cryptography]==3.3.0    # JWT 토큰 생성 및 검증

# =================================================================
# 🌐 HTTP 클라이언트 & 비동기 처리
# =================================================================
httpx==0.25.2                       # 비동기 HTTP 클라이언트
aiohttp==3.9.1                      # 대안 비동기 HTTP 클라이언트
aiofiles==23.2.1                    # 비동기 파일 I/O
asyncio-mqtt==0.16.1                # 비동기 MQTT 클라이언트 (선택사항)

# =================================================================
# 📊 Google API & 서비스 연동
# =================================================================
google-api-python-client==2.108.0   # Google API 클라이언트 라이브러리
google-auth-httplib2==0.1.1         # Google 인증 HTTP 라이브러리
google-auth-oauthlib==1.1.0         # Google OAuth2 인증
google-auth==2.25.2                 # Google 인증 라이브러리
gspread==5.12.0                     # Google Sheets API 클라이언트
oauth2client==4.1.3                 # OAuth2 클라이언트 (legacy)
google-generativeai==0.3.2          # Google Gemini AI API

# =================================================================
# 📈 금융 데이터 & 트레이딩 API
# =================================================================
alpaca-trade-api==3.1.1             # Alpaca 거래소 API
yfinance==0.2.28                    # Yahoo Finance 데이터
pandas-ta==0.3.14b0                 # 기술적 지표 계산 라이브러리
ta==0.10.2                          # 기술적 분석 지표
ccxt==4.1.92                        # 암호화폐 거래소 통합 API (선택사항)

# =================================================================
# 📊 데이터 처리 & 분석
# =================================================================
pandas==2.1.3                       # 데이터 조작 및 분석
numpy==1.26.2                       # 수치 계산 라이브러리
scipy==1.11.4                       # 과학 계산 라이브러리
scikit-learn==1.3.2                 # 머신러닝 라이브러리
matplotlib==3.8.2                   # 차트 생성 (백엔드용)
plotly==5.17.0                      # 인터랙티브 차트 (선택사항)

# =================================================================
# 🗄️ 데이터베이스 & 캐싱
# =================================================================
sqlalchemy==2.0.23                  # SQL 데이터베이스 ORM
alembic==1.13.1                     # 데이터베이스 마이그레이션
redis==5.0.1                        # Redis 클라이언트 (캐싱용)
hiredis==2.2.3                      # Redis 고성능 파서

# =================================================================
# ⚙️ 설정 & 환경 변수 관리
# =================================================================
python-dotenv==1.0.0                # .env 파일 환경변수 로드
pydantic==2.5.0                     # 데이터 검증 및 설정 관리
pydantic-settings==2.1.0            # Pydantic 설정 확장

# =================================================================
# 🔐 보안 & 인증
# =================================================================
cryptography==41.0.8                # 암호화 라이브러리
passlib[bcrypt]==1.7.4              # 비밀번호 해싱
python-multipart==0.0.6             # OAuth2 폼 데이터 처리
itsdangerous==2.1.2                 # 보안 토큰 생성

# =================================================================
# 📅 스케줄링 & 백그라운드 작업
# =================================================================
apscheduler==3.10.4                 # 작업 스케줄러
celery==5.3.4                       # 분산 작업 큐 (선택사항)
dramatiq[redis]==1.15.0             # 가벼운 작업 큐 (선택사항)

# =================================================================
# 📝 로깅 & 모니터링
# =================================================================
loguru==0.7.2                       # 개선된 로깅 라이브러리
sentry-sdk[fastapi]==1.38.0         # 에러 추적 (선택사항)
prometheus-client==0.19.0           # 메트릭 수집 (선택사항)

# =================================================================
# 🧪 테스팅 & 개발 도구
# =================================================================
pytest==7.4.3                       # 테스트 프레임워크
pytest-asyncio==0.21.1              # 비동기 테스트 지원
httpx==0.25.2                       # 테스트용 HTTP 클라이언트
faker==20.1.0                       # 더미 데이터 생성
factory-boy==3.3.0                  # 테스트 데이터 팩토리

# =================================================================
# 🔧 유틸리티 & 헬퍼
# =================================================================
python-dateutil==2.8.2              # 날짜 처리 유틸리티
pytz==2023.3.post1                  # 타임존 처리
requests==2.31.0                    # HTTP 클라이언트 (동기)
urllib3==2.1.0                      # HTTP 라이브러리
jinja2==3.1.2                       # 템플릿 엔진

# =================================================================
# 📊 스트리밍 & 실시간 데이터
# =================================================================
websocket-client==1.6.4             # WebSocket 클라이언트
python-socketio==5.10.0             # Socket.IO 클라이언트
eventlet==0.33.3                    # 네트워킹 라이브러리

# =================================================================
# 🎯 AI & 머신러닝 (선택사항)
# =================================================================
openai==1.3.7                       # OpenAI API (Gemini 대안)
transformers==4.36.2                # 허깅페이스 트랜스포머 (선택사항)
torch==2.1.1                        # PyTorch (선택사항)

# =================================================================
# 📦 패키지 관리 & 빌드
# =================================================================
wheel==0.42.0                       # 휠 빌드 도구
setuptools==69.0.2                  # 패키지 빌드 도구

# =================================================================
# 📱 개발 환경 개선 (개발용 - 선택사항)
# =================================================================
# black==23.11.0                    # 코드 포맷터
# isort==5.12.0                     # import 정렬
# flake8==6.1.0                     # 린터
# mypy==1.7.1                       # 타입 체커
# pre-commit==3.6.0                 # Git 훅 관리

# =================================================================
# 🚨 중요 노트
# =================================================================
# 1. 개발 환경에서만 필요한 패키지들은 주석 처리되어 있습니다
# 2. 선택사항 패키지들은 필요에 따라 주석 해제하여 사용하세요
# 3. 프로덕션 환경에서는 보안 관련 패키지들을 반드시 포함하세요
# 4. 정기적으로 패키지 버전을 업데이트하여 보안 패치를 적용하세요

# =================================================================
# 설치 후 확인 방법:
# pip list | grep -E "(fastapi|pandas|alpaca|google)"
# =================================================================
